diff --git a/tools/generators/legacy/src/Generator/CreateXCUserData.swift b/tools/generators/legacy/src/Generator/CreateXCUserData.swift
index b8890644..449d50ca 100644
--- a/tools/generators/legacy/src/Generator/CreateXCUserData.swift
+++ b/tools/generators/legacy/src/Generator/CreateXCUserData.swift
@@ -24,11 +24,12 @@ extension Generator {
         let schemeUserStates = (customSchemes + sortedAutogeneratedSchemes)
             .enumerated()
             .map { index, scheme in
+                let isShown = !(scheme.name.hasSuffix("_objc") || scheme.name.hasSuffix("_swift"))
                 return XCSchemeManagement.UserStateScheme(
                     name: "\(scheme.name).xcscheme",
                     shared: true,
                     orderHint: index,
-                    isShown: true
+                    isShown: isShown
                 )
             }
         
